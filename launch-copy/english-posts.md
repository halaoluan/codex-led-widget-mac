# English Launch Copy

## X / Twitter Teaser

```text
I got tired of opening the Codex quota page over and over, so I built a tiny macOS desktop widget for it.

Green = available
Yellow = low
Red = exhausted

It reads quota locally from the installed Codex app.

GitHub:
https://github.com/halaoluan/codex-led-widget-mac
```

## X / Twitter Demo Post

```text
Built a small macOS desktop widget for Codex quota.

It shows:
- remaining quota
- reset countdowns
- 5-hour and 7-day windows
- green/yellow/red status
- small/medium/large sizes

Open source:
https://github.com/halaoluan/codex-led-widget-mac
```

## Show HN

Title:

```text
Show HN: I made a macOS widget to track my Codex quota
```

First comment:

```text
I use Codex heavily and kept opening the quota page many times a day, so I built a small macOS desktop widget that shows the remaining quota at a glance.

Green means available, yellow means low, red means exhausted. It reads quota locally from the installed Codex app and does not upload account data.

Would love feedback from other Codex users, especially on macOS desktop behavior.

GitHub:
https://github.com/halaoluan/codex-led-widget-mac
```

## Product Hunt

Name:

```text
Codex LED Widget
```

Tagline:

```text
A macOS desktop widget for tracking your Codex quota
```

Description:

```text
Codex LED Widget keeps your local Codex quota visible on the macOS desktop. It uses a simple green/yellow/red status indicator, shows reset countdowns, supports Chinese and English, and reads quota locally from the installed Codex app.
```

Maker comment:

```text
I built Codex LED Widget because I kept checking the quota page while working with Codex. This keeps the quota state on the macOS desktop with a simple green/yellow/red signal, and it reads quota locally from the installed Codex app.

I would love feedback from other Codex users, especially on installation, desktop behavior, and what should be added next.
```

## Reddit

Title option 1:

```text
I built a small macOS widget for tracking Codex quota
```

Title option 2:

```text
Tiny macOS desktop widget that shows local Codex quota
```

Post:

```text
I use Codex heavily and kept checking the quota page many times a day, so I built a small macOS desktop widget for it.

It shows the remaining quota, 5-hour and 7-day windows, reset countdowns, and a simple green/yellow/red status.

It reads quota locally from the installed Codex app and does not upload account data.

GitHub:
https://github.com/halaoluan/codex-led-widget-mac

I would appreciate feedback from other macOS/Codex users, especially around installation and desktop behavior.
```
